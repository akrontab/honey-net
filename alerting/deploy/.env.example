# Alerting detector environment — generated by scripts/deploy_detector.py
# Copy to .env and fill in values if running manually.

# Tailscale IP of the log-stack host (Loki runs here)
LOKI_HOST=

# Full URL of the malware-catalog API (e.g. http://100.x.y.z:8000)
CATALOG_URL=
