# Tailscale IP of the log-stack host.
# Copy this file to .env and fill in the value before starting Vector.
# Get the IP from the log-stack host after setup.sh runs: tailscale ip -4
LOKI_HOST=100.x.x.x

# Optional: override the hostname label attached to logs in Grafana.
HONEYPOT_HOSTNAME=mysql-honeypot
